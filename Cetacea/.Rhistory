res <- mcmc.prosse(lik=lik, tree=tree, species.name=species.name, unknown.tip=unknown.tip, x.init=x.init,
nstepsw=30,nsteps=5000,w=rep(1,3),prior=prior,lower=rep(0,3),upper=c(Inf,Inf,Inf))
#mcmc_trace <- capture.output(res <- mcmc.prosse(lik=lik, tree=tree, species.name=species.name, unknown.tip=unknown.tip, x.init=x.init,
#                                                nstepsw=30,nsteps=5000,w=rep(1,3),prior=prior,lower=rep(0,3),upper=c(Inf,Inf,Inf)))
res <- mcmc.prosse(lik=lik, tree=tree, species.name=species.name, unknown.tip=unknown.tip, x.init=x.init,
nstepsw=30,nsteps=5000,w=rep(1,3),prior=prior,lower=rep(0,3),upper=c(Inf,Inf,Inf))
#mcmc_trace <- capture.output(res <- mcmc.prosse(lik=lik, tree=tree, species.name=species.name, unknown.tip=unknown.tip, x.init=x.init,
#                                                nstepsw=30,nsteps=5000,w=rep(1,3),prior=prior,lower=rep(0,3),upper=c(Inf,Inf,Inf)))
res <- mcmc.prosse(lik=lik, tree=tree, species.name=species.name, unknown.tip=unknown.tip, x.init=x.init,
nstepsw=30,nsteps=5000,w=rep(1,3),prior=prior,lower=rep(0,3),upper=c(Inf,Inf,Inf))
#devtools::install("diversitree", dependencies=T)
#install.packages("diversitree", repos = NULL, type="source")
#install.packages("diversitree", repos = NULL, type = "source", INSTALL_opts = c('--no-multiarch', '--debug'))
library(diversitree)
#library(Rcpp)
#source("diversitree/R/drop.tip.fixed.R")
pars <- c(b=0.4,mu=0.1,lambda=0.1)
tree <- tree.prosse(pars=pars,max.t=15)
names(tree$species) <- tree$tip.label
n.unknown.tip <- round(length(tree$tip.label)*0.1)
unknown.tip <- sample(x=tree$tip.label,size=n.unknown.tip)
tree$species[unknown.tip] <- 0
species <- unique(tree$species)
tree$species[unknown.tip] <- c(1:n.unknown.tip)+max(species)
species.name <- tree$species[unknown.tip]
plot(tree)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
p <- starting.point.prosse(tree, lik, q.div=5)
x.init <- c(p,tree$species[unknown.tip])
x.init
#devtools::install("diversitree", dependencies=T)
#install.packages("diversitree", repos = NULL, type="source")
#install.packages("diversitree", repos = NULL, type = "source", INSTALL_opts = c('--no-multiarch', '--debug'))
library(diversitree)
#library(Rcpp)
#source("diversitree/R/drop.tip.fixed.R")
pars <- c(b=0.4,mu=0.1,lambda=0.1)
tree <- tree.prosse(pars=pars,max.t=15)
names(tree$species) <- tree$tip.label
n.unknown.tip <- round(length(tree$tip.label)*0.1)
unknown.tip <- sample(x=tree$tip.label,size=n.unknown.tip)
tree$species[unknown.tip] <- 0
species <- unique(tree$species)
tree$species[unknown.tip] <- c(1:n.unknown.tip)+max(species)
species.name <- tree$species[unknown.tip]
plot(tree)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
p <- starting.point.prosse(tree, lik, q.div=5)
x.init <- c(p,tree$species[unknown.tip])
x.init
#devtools::install("diversitree", dependencies=T)
#install.packages("diversitree", repos = NULL, type="source")
#install.packages("diversitree", repos = NULL, type = "source", INSTALL_opts = c('--no-multiarch', '--debug'))
library(diversitree)
#library(Rcpp)
#source("diversitree/R/drop.tip.fixed.R")
pars <- c(b=0.4,mu=0.1,lambda=0.1)
tree <- tree.prosse(pars=pars,max.t=15)
names(tree$species) <- tree$tip.label
n.unknown.tip <- round(length(tree$tip.label)*0.1)
unknown.tip <- sample(x=tree$tip.label,size=n.unknown.tip)
tree$species[unknown.tip] <- 0
species <- unique(tree$species)
tree$species[unknown.tip] <- c(1:n.unknown.tip)+max(species)
species.name <- tree$species[unknown.tip]
plot(tree)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
p <- starting.point.prosse(tree, lik, q.div=5)
x.init <- c(p,tree$species[unknown.tip])
x.init
#devtools::install("diversitree", dependencies=T)
#install.packages("diversitree", repos = NULL, type="source")
#install.packages("diversitree", repos = NULL, type = "source", INSTALL_opts = c('--no-multiarch', '--debug'))
library(diversitree)
#library(Rcpp)
#source("diversitree/R/drop.tip.fixed.R")
pars <- c(b=0.4,mu=0.1,lambda=0.1)
tree <- tree.prosse(pars=pars,max.t=15)
names(tree$species) <- tree$tip.label
n.unknown.tip <- round(length(tree$tip.label)*0.1)
unknown.tip <- sample(x=tree$tip.label,size=n.unknown.tip)
tree$species[unknown.tip] <- 0
species <- unique(tree$species)
tree$species[unknown.tip] <- c(1:n.unknown.tip)+max(species)
species.name <- tree$species[unknown.tip]
plot(tree)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
p <- starting.point.prosse(tree, lik, q.div=5)
x.init <- c(p,tree$species[unknown.tip])
x.init
#devtools::install("diversitree", dependencies=T)
#install.packages("diversitree", repos = NULL, type="source")
#install.packages("diversitree", repos = NULL, type = "source", INSTALL_opts = c('--no-multiarch', '--debug'))
library(diversitree)
#library(Rcpp)
#source("diversitree/R/drop.tip.fixed.R")
pars <- c(b=0.4,mu=0.1,lambda=0.1)
tree <- tree.prosse(pars=pars,max.t=15)
names(tree$species) <- tree$tip.label
n.unknown.tip <- round(length(tree$tip.label)*0.1)
unknown.tip <- sample(x=tree$tip.label,size=n.unknown.tip)
tree$species[unknown.tip] <- 0
species <- unique(tree$species)
tree$species[unknown.tip] <- c(1:n.unknown.tip)+max(species)
species.name <- tree$species[unknown.tip]
plot(tree)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
p <- starting.point.prosse(tree, lik, q.div=5)
x.init <- c(p,tree$species[unknown.tip])
x.init
#devtools::install("diversitree", dependencies=T)
#install.packages("diversitree", repos = NULL, type="source")
#install.packages("diversitree", repos = NULL, type = "source", INSTALL_opts = c('--no-multiarch', '--debug'))
library(diversitree)
#library(Rcpp)
#source("diversitree/R/drop.tip.fixed.R")
pars <- c(b=0.4,mu=0.1,lambda=0.1)
tree <- tree.prosse(pars=pars,max.t=15)
names(tree$species) <- tree$tip.label
n.unknown.tip <- round(length(tree$tip.label)*0.1)
unknown.tip <- sample(x=tree$tip.label,size=n.unknown.tip)
tree$species[unknown.tip] <- 0
species <- unique(tree$species)
tree$species[unknown.tip] <- c(1:n.unknown.tip)+max(species)
species.name <- tree$species[unknown.tip]
plot(tree)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
p <- starting.point.prosse(tree, lik, q.div=5)
x.init <- c(p,tree$species[unknown.tip])
x.init
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_MCC.tre")
treeM
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_mcc.tree")
treeM
treeM$tip.label
plot(treeM)
is.rooted(treeM)
treeM$tip.label
gsub("'","",treeM$tip.label)
treeM$node.label
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_mcc.tree")
# Remove Dendigloborotalia anfracta and Neigatllitellia vivans, who have been identified as distinct from the monophyletic microperforate group (Morard et al. 2019, https://doi.org/10.1371/journal.pone.0213936), and Globigerinita minuta, that is identified as even more basal in this analysis.
#treeM <- drop.tip(treeM, tip = grep("minuta|vivans|anfracta", treeM$tip.label, value=T))
# Root the tree
#treeM <- midpoint.root(treeM)
treeM$node.label <- paste0("node_", 1:length(treeM$node.label))
plot(treeM, cex = 0.7, show.node.label=T)
axisPhylo()
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_mcc.tree")
# Remove Dendigloborotalia anfracta and Neigatllitellia vivans, who have been identified as distinct from the monophyletic microperforate group (Morard et al. 2019, https://doi.org/10.1371/journal.pone.0213936), and Globigerinita minuta, that is identified as even more basal in this analysis.
#treeM <- drop.tip(treeM, tip = grep("minuta|vivans|anfracta", treeM$tip.label, value=T))
# Root the tree
#treeM <- midpoint.root(treeM)
treeM$node.label <- paste0("node_", 1:length(treeM$node.label))
plot(treeM, cex = 0.7, show.node.label=T)
axisPhylo()
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_mcc.tree")
# Remove Dendigloborotalia anfracta and Neigatllitellia vivans, who have been identified as distinct from the monophyletic microperforate group (Morard et al. 2019, https://doi.org/10.1371/journal.pone.0213936), and Globigerinita minuta, that is identified as even more basal in this analysis.
#treeM <- drop.tip(treeM, tip = grep("minuta|vivans|anfracta", treeM$tip.label, value=T))
# Root the tree
#treeM <- midpoint.root(treeM)
treeM$node.label <- paste0("node_", 1:length(treeM$node.label))
plot(treeM, cex = 0.7, show.node.label=T)
axisPhylo()
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_mcc.tree")
# Remove Dendigloborotalia anfracta and Neigatllitellia vivans, who have been identified as distinct from the monophyletic microperforate group (Morard et al. 2019, https://doi.org/10.1371/journal.pone.0213936), and Globigerinita minuta, that is identified as even more basal in this analysis.
#treeM <- drop.tip(treeM, tip = grep("minuta|vivans|anfracta", treeM$tip.label, value=T))
# Root the tree
#treeM <- midpoint.root(treeM)
treeM$node.label <- paste0("node_", 1:length(treeM$node.label))
plot(treeM, cex = 0.7, show.node.label=T)
axisPhylo()
treeM$species
treeM$tip.label
sapply(treeM$tip.label, function(full_name)paste(strsplit(full_name, "\\|")[[1]][3:4], collapse="_"))
sapply(treeM$tip.label, function(full_name)paste(strsplit(full_name, "\\_")[[1]][3:4], collapse="_"))
sapply(treeM$tip.label, function(full_name)paste(strsplit(full_name, "\\_")[[1]][1:2], collapse="_"))
sapply(treeM$tip.label, function(full_name)paste(strsplit(full_name, "\\_")[[1]][1], collapse="_"))
sapply(treeM$tip.label, function(full_name)paste(strsplit(full_name, "\\_")[[1]][1:2], collapse="_"))
gsub("\\_I*", "", treeM$tip.label)
gsub("\\(_I)*", "", treeM$tip.label)
gsub("\\(_I*)", "", treeM$tip.label)
gsub("(_I*)", "", treeM$tip.label)
gsub("_I*", "", treeM$tip.label)
gsub("\\_I*", "", treeM$tip.label)
gsub("\\_I*", "", treeM$tip.label)treeM$tip.label
treeM$tip.label
gsub("_I.*", "", treeM$tip.label)
# Extract the morphospecies name
treeM$species <- gsub("_I.*", "", treeM$tip.label)
names(treeM$species)
treeM$tip.label
names(treeM$species) <- treeM$tip.label
unique(treeM$species)
treeM$species
treeM$species
unique(treeM$species)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM)
tree$root.depth
max(depth)
tree$root.depth
tree = treeM
edge <- tree$edge
edge.length <- tree$edge.length
idx <- seq_len(max(edge))
n.tip <- length(tree$tip.label)
tips <- seq_len(n.tip)
root <- n.tip + 1
is.tip <- idx <= n.tip
children <- diversitree:::get.children(edge, n.tip)
parent <- edge[match(idx, edge[, 2]), 1]
order <- order.tmp <- diversitree:::get.ordering(children,
is.tip, root)
species <- tree$species
species.label <- unique(species)
n.species <- length(species.label)
tip.species <- sapply(species.label, function(i) is.element(species,
i))
n.tip.species <- colSums(tip.species)
stars <- numeric(2 * n.tip - 1) + 2
stars[1:n.tip] <- 1
stars[n.tip.species == 1] <- 2
group <- lapply(which(n.tip.species == 1), function(i) which(tip.species[,
i]))
clade <- which(n.tip.species > 1)
if (length(clade) > 0) {
mono <- sapply(clade, function(i) is.monophyletic(tree,
which(tip.species[, i])))
if (length(mono) > 0) {
group.tmp <- lapply(clade[mono], function(i) edge[max(which(is.element(edge[,
2], which(tip.species[, i])))) - c(0:(n.tip.species[i] *
2 - 2)), 2])
stars[unlist(lapply(group.tmp, function(i) i[-length(i)]))] <- 1
stars[unlist(lapply(group.tmp, function(i) i[length(i)]))] <- 2
group <- c(group, group.tmp)
}
anc <- vector("list", max(order))
for (i in c(rev(order[-length(order)]), tips)) {
anc[[i]] <- c(parent[i], anc[[parent[i]]])
}
exc <- lapply(clade[!mono], function(i) Reduce(intersect,
anc[which(tip.species[, i])]))
if (length(exc) > 0) {
nodes <- lapply(1:length(exc), function(i) unlist(anc[which(tip.species[,
clade[!mono][i]])]))
group.tmp <- lapply(1:length(exc), function(i) unique(c(which(tip.species[,
clade[!mono][i]]), sort(nodes[[i]][!is.element(nodes[[i]],
exc[[i]][-1])], decreasing = T))))
group.tmp2 <- unlist(group.tmp)
group.root <- sapply(exc, max, na.rm = T)
stars[group.root] <- 2
stars[group.tmp2] <- 1
}
names(group.tmp) <- names(exc)
if (length(exc) > 0) {
tmp <- lapply(group, function(i) anc[[i[length(i)]]])
tmp2 <- lapply(tmp, function(i) which(is.element(i,
group.tmp2)))
tmp3 <- which(sapply(tmp2, length) > 0)
if (length(tmp3) > 0) {
tmp <- unlist(lapply(tmp3, function(i) tmp[[i]][0:(tmp2[[i]][1] -
1)]))
tmp <- order[is.element(order, tmp)]
}
else {
tmp <- NULL
}
tmp_para <- lapply(group.tmp, function(i) anc[[i[length(i)]]])
tmp2 <- lapply(1:length(tmp_para), function(i) which(is.element(tmp_para[[i]],
unlist(group.tmp[-i]))))
tmp3 <- which(sapply(tmp2, length) > 0)
if (length(tmp3) > 0) {
tmp_para <- unlist(lapply(tmp3, function(i) tmp_para[[i]][0:(tmp2[[i]][1] -
1)]))
tmp_para <- order[is.element(order, tmp_para)]
}
else {
tmp_para <- NULL
}
tmp <- unique(c(tmp, tmp_para))
if (length(tmp) > 0) {
tmp <- as.list(tmp)
}
group <- c(group, tmp, group.tmp)
if (length(tmp) + (length(group.tmp)) > 1) {
i <- length(group) - length(group.tmp) - length(tmp) +
1
while (i < length(group)) {
x <- 0
tmp2 <- group[[i]]
tmp2 <- tmp2[tmp2 > n.tip]
for (j in 1:length(tmp2)) {
if (prod(is.element(children[tmp2[j], ],
unlist(group[1:i]))) == 0) {
group <- c(group[-i], group[i])
x <- 1
break
}
}
if (x == 0) {
i <- i + 1
}
}
}
order <- order[!is.element(order, c(unlist(group),
tmp))]
}
}
len <- edge.length[match(idx, edge[, 2])]
height <- diversitree:::branching.heights(tree)
depth <- max(height) - height
len[root] <- tree$root.depth - max(depth)
if (is.ultrametric(tree))
max(depth)
tree$root.depth
tree$root.depth - max(depth)
tree$root.depth
node.depth(treeM)
node.height(treeM-)
unique(treeM$specie)
node.height(treeM)
castor::get_all_node_depths(treeM)
treeM$root.edge
treeM <- read.tree("../../4-Phylogenetic_reconstruction/output/forams_Microperforate_mcc.tree")
# Remove Dendigloborotalia anfracta and Neigatllitellia vivans, who have been identified as distinct from the monophyletic microperforate group (Morard et al. 2019, https://doi.org/10.1371/journal.pone.0213936), and Globigerinita minuta, that is identified as even more basal in this analysis.
#treeM <- drop.tip(treeM, tip = grep("minuta|vivans|anfracta", treeM$tip.label, value=T))
# Root the tree
#treeM <- midpoint.root(treeM)
treeM$node.label <- paste0("node_", 1:length(treeM$node.label))
treeM$root.depth <- max(castor::get_all_node_depths(treeM))
plot(treeM, cex = 0.7, show.node.label=T)
axisPhylo()
# Extract the morphospecies name
treeM$species <- gsub("_I.*", "", treeM$tip.label)
names(treeM$species) <- treeM$tip.label
unique(treeM$species)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM)
x.init <- starting.point.prosse(treeM)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM, lik)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM, lik=lik)
lik <- make.prosse(treeM, )
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM)
x.init <- starting.point.prosse(treeM)
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM)
x.init <- starting.point.prosse(treeM, lik)
# mcmc_trace <- capture.output(resM <- mcmc.prosse(lik=lik, tree=treeM, species.name=c(), unknown.tip=c(), x.init=x.init,
#                    nstepsw=30, nsteps=5000, w=rep(1,3), prior=prior, lower=rep(0,3), upper=c(Inf,Inf,Inf)))
x.init
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM)
x.init <- starting.point.prosse(treeM, lik)
# mcmc_trace <- capture.output(resM <- mcmc.prosse(lik=lik, tree=treeM, species.name=c(), unknown.tip=c(), x.init=x.init,
#                    nstepsw=30, nsteps=5000, w=rep(1,3), prior=prior, lower=rep(0,3), upper=c(Inf,Inf,Inf)))
prior <- make.prior.exponential(0.5)
lik <- make.prosse(treeM)
x.init <- starting.point.prosse(treeM, lik)
mcmc_trace <- capture.output(resM <- mcmc.prosse(lik=lik, tree=treeM, species.name=c(), unknown.tip=c(), x.init=x.init,
nstepsw=30, nsteps=5000, w=rep(1,3), prior=prior, lower=rep(0,3), upper=c(Inf,Inf,Inf)))
write.tree(treeM, "output/forams_Microperforate_mcc_prosse.tree")
tree_cp <- treeM
tree_cp
tree_cp$tip.label
tree <- read.tree("~/Downloads/phylo_incipient.tree")
plot(tree)
tree$species <- gsub("_I.*", "", tree$tip.label)
tree$species
names(treeM$species) <- treeM$tip.label
tree <- read.tree("~/Downloads/phylo_incipient.tree")
tree$species <- gsub("_I.*", "", tree$tip.label)
names(treeM$species) <- treeM$tip.label
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
tree$root.depth
midpoint.root(tree)$root.depth
??midpoint.root
phytools::midpoint.root(tree)$root.depth
phytools::midpoint.root(tree)
max(castor::get_all_node_depths(tree))
tree <- read.tree("~/Downloads/phylo_incipient.tree")
tree$species <- gsub("_I.*", "", tree$tip.label)
tree$root.depth <- max(castor::get_all_node_depths(tree))
names(tree$species) <- tree$tip.label
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
make.prosse
tree <- read.tree("~/Downloads/phylo_incipient.tree")
tree$species <- gsub("_I.*", "", tree$tip.label)
tree$root.depth <- max(castor::get_all_node_depths(tree))
names(tree$species) <- tree$tip.label
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
x.init <- starting.point.prosse(tree, lik)
mcmc_trace <- capture.output(resM <- mcmc.prosse(lik=lik, tree=tree, species.name=c(), unknown.tip=c(), x.init=x.init,
nstepsw=30, nsteps=5000, w=rep(1,3), prior=prior, lower=rep(0,3), upper=c(Inf,Inf,Inf)))
write.tree(tree, "~/Downloads/phylo_incipient.tree")
tree <- read.tree("~/Downloads/phylo_incipient.tree")
tree <- read.tree("~/Downloads/phylo_incipient.tree")
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
tree
plot(tree)
tree$tip.label
tree$species
tree <- read.tree("~/Downloads/phylo_incipient.tree")
tree$species <- gsub("_I.*", "", tree$tip.label)
tree$root.depth <- max(castor::get_all_node_depths(tree))
names(tree$species) <- tree$tip.label
prior <- make.prior.exponential(0.5)
lik <- make.prosse(tree)
x.init <- starting.point.prosse(tree, lik)
mcmc_trace <- capture.output(resM <- mcmc.prosse(lik=lik, tree=tree, species.name=c(), unknown.tip=c(), x.init=x.init,
nstepsw=30, nsteps=5000, w=rep(1,3), prior=prior, lower=rep(0,3), upper=c(Inf,Inf,Inf)))
write.tree(tree, "~/Downloads/phylo_incipient.tree")
read.tree("~/Downloads/phylo_incipient.tree")$species
save(tree, "~/Downloads/phylo_incipient.Rdata")
?save
save(tree, file = "~/Downloads/phylo_incipient.Rdata")
tree 1
tree <- 1
"~/Downloads/phylo_incipient.Rdata"
load("~/Downloads/phylo_incipient.Rdata")
tree
tree$root.depth
tree$species
# Load necessary libraries
library(readr)
library(lubridate)
library(coda)
setwd("~/Nextcloud/Recherche/1_Methods/INSANE/OBDP/Cetacea_OBD/")
# Extract timestamps and calculate duration
calculate_duration <- function(log_file) {
# Read all lines from the log file
log_lines <- read_lines(log_file)
# Extract all lines that contain timestamps
timestamp_lines <- grep("\\(\\d+\\.\\d+\\.\\d+\\) \\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}", log_lines, value = TRUE)
# Extract the timestamps from these lines
timestamps <- sub(".*\\((\\d+\\.\\d+\\.\\d+)\\) (\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}).*", "\\2", timestamp_lines)
# Convert the timestamps to POSIXct format
time_values <- ymd_hms(timestamps)
# Check if "Job terminated" is in the log file
if (any(grepl("Job terminated|Job exceeded its runtime limit", log_lines))) {
# Extract the timestamp of the job termination
end_time <- max(time_values)
} else {
# If the job is still running, use the current time
end_time <- Sys.time()
}
# Calculate the duration between the first timestamp and the end time in hours
duration <- difftime(end_time, min(time_values), units = "days")
return(as.numeric(duration))
}
# Compute ESS from a log file
compute_ess <- function(trace_file) {
# Read the trace data from the log file
trace_data <- read.table(trace_file, header = TRUE)
trace_data_reduced <- trace_data[!grepl("iteration|div|ne|ns", names(trace_data))]
trace_data_reduced <- trace_data_reduced[colSums(sapply(trace_data_reduced, diff)) != 0]
# Compute the ESS
#ess_value <- effectiveSize(as.mcmc(trace_data$likelihood))
ess_value <- mean(sapply(trace_data_reduced, effectiveSize))
return(as.numeric(ess_value))
}
# Compute the ESS/day for each replicates
seeds <- 0:4
get_essperday <- function(log_path, trace_path){
results <- sapply(seeds, function(seed) {
log_file <- paste0(log_path, seed, ".log")
trace_file <- paste0(trace_path, seed, ".log")
ess <- compute_ess(trace_file)
duration <- calculate_duration(log_file)
ess_per_day <- ess / duration
# Append the results to the data frame
data.frame(Seed = seed, Duration_d = duration, ESS = ess, ESS_per_day = ess_per_day)
})
return (cbind(results, c("Average", apply(results[-1,], 1, function(x)(mean(as.numeric(x)))))))
}
## CBD
results_CBD <- get_essperday("Logs/CBD.14464.", "outputs/CBD_Cetaceans_2000000iter_seed")
results_CBD
## CFBD
results_CFBD <- get_essperday("Logs/CFBD.14462.", "outputs/CFBD_Cetaceans_2000000iter_seed")
results_CFBD
## COBD
results_COBD <- get_essperday("Logs/COBD.14463.", "outputs/COBD_Cetaceans_2000000iter_seed")
results_COBD
## COBD_rmFossils
results_COBD_rmFossils <- get_essperday("Logs/COBD_rmFossils.14485.", "outputs/COBD_rmFossils_Cetaceans_50000000iter_seed")
results_COBD_rmFossils
## BDD
results_BDD <- get_essperday("Logs/BDD.14472.", "outputs/BDD_Cetaceans_10000000iter_seed")
results_BDD
## FBDD
results_FBDD <- get_essperday("Logs/FBDD.14467.", "outputs/FBDD_Cetaceans_rateαprior0.1_10000000iter_seed")
results_FBDD
## OBDD
results_OBDD <- get_essperday("Logs/OBDD.14466.", "outputs/OBDD_Cetaceans_rateαprior0.1_10000000iter_seed")
results_OBDD
## OBDD_rmFossils
results_OBDD_rmFossils <- get_essperday("Logs/OBDD_rmFossils.14460.", "outputs/OBDD_rmFossils_Cetaceans_rateαprior0.1_20000000iter_seed")
results_OBDD_rmFossils
